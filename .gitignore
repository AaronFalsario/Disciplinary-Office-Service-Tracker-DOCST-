# ============ SUPABASE & API KEYS (CRITICAL - NEVER COMMIT) ============
supabase.js
supabase-key.js
supabase-credentials.js
*-secret.js
*credentials.js
*.key
*.pem

# ============ ENVIRONMENT VARIABLES ============
.env
.env.local
.env.production
.env.development
.env.*.local

# ============ DEPENDENCIES ============
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
# NOTE: Keep package-lock.json — Vercel needs it for reproducible installs
yarn.lock
pnpm-lock.yaml

# ============ BUILD OUTPUTS ============
dist/
build/
out/

# ============ TEMPORARY FILES ============
*.log
*.tmp
*.temp
*.cache
.cache/
temp/
tmp/
*.backup
*.bak
*.old

# ============ BACKUP & CREDENTIAL FILES ============
**/*.backup.js
**/*.credentials.js
**/*-secret.js
**/secret-*.js
**/config.local.*
**/credentials.local.*

# ============ OS GENERATED FILES ============
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
Desktop.ini

# ============ EDITOR & IDE FILES ============
.vscode/
.idea/
*.swp
*.swo
*~
.project
.classpath
.settings/
*.sublime-*

# ============ LOCAL STORAGE & DATABASE ============
*.localstorage
*.localstorage-journal
*.sqlite
*.db
*.sqlite3

# ============ SESSION & AUTH DATA ============
session/
sessions/
*.session
jwt-*
auth-token-*

# ============ TEST COVERAGE ============
coverage/
.nyc_output/
*.test-result
test-results/

# ============ DEPLOYMENT & HOSTING ============
.vercel/
.netlify/
.firebase/
.aws-sam/
.parcel-cache/

# ============ ADMIN DASHBOARD SPECIFIC ============
Assets/Admin dashboard/config.local.js
Assets/Admin dashboard/backup-*.js
AUTH/temp-*.json
AUTH/*-backup.js

# ============ USER UPLOADS (if any) ============
uploads/
temp-uploads/
uploads/*.csv

# ============ MISC ============
*.gz
*.zip
*.rar
*.7z
*.tar